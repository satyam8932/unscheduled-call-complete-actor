{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "Unscheduled Call Complete Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "subAccountUrl": {
            "title": "Sub Account URL",
            "type": "string",
            "description": "Full URL of the GHL sub-account dashboard",
            "editor": "textfield"
        },
        "leadName": {
            "title": "Lead Name",
            "type": "string",
            "description": "Name of the lead to search for in global search",
            "editor": "textfield"
        },
        "storageState": {
            "title": "Storage State",
            "type": "object",
            "description": "Browser storageState JSON (cookies + localStorage). Export from loginMode run.",
            "editor": "json"
        },
        "loginMode": {
            "title": "Login Mode",
            "type": "boolean",
            "description": "Set to true to open browser for manual login. Saves session for future runs.",
            "editor": "checkbox",
            "default": false
        }
    },
    "required": ["subAccountUrl", "leadName"]
}
